# Core Dependencies
torch>=2.1.0,<2.2.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers and Model Libraries
transformers>=4.45.0
tokenizers>=0.15.0
datasets>=2.16.0
evaluate>=0.4.1

# Axolotl Framework and Dependencies
# Note: Axolotl should be installed from source via train_axolotl.sh
peft>=0.8.0
bitsandbytes>=0.42.0
accelerate>=0.26.0
deepspeed>=0.12.0

# Attention and Memory Optimization
flash-attn>=2.4.0
xformers>=0.0.23  # Alternative to flash attention

# Image Processing
Pillow>=10.1.0
opencv-python>=4.8.0

# Data Processing
numpy>=1.24.0
pandas>=1.5.0
sentencepiece>=0.1.99
safetensors>=0.4.0

# Training and Monitoring
wandb>=0.16.0
tensorboard>=2.15.0
tensorboardX>=2.6.0

# Quantization
auto-gptq>=0.6.0  # For GPTQ quantization support

# Utility Libraries
PyYAML>=6.0.1
tqdm>=4.66.0
click>=8.1.0
rich>=13.7.0
omegaconf>=2.3.0

# Model Hub and Storage
huggingface-hub>=0.20.0
azure-storage-blob>=12.19.0  # For Azure storage if needed
boto3>=1.34.0  # For AWS S3 if needed

# Audio Processing (if needed for multimodal)
librosa>=0.10.0
soundfile>=0.12.0

# Additional Dependencies from Original
runpod>=1.6.0  # For deployment compatibility
pymupdf>=1.23.0  # For PDF handling
vector_quantize_pytorch>=1.14.0  # For vector quantization
vocos>=0.1.0  # For audio processing

# Development and Testing
pytest>=7.4.0
black>=23.12.0
isort>=5.13.0
flake8>=7.0.0

# Optional: For distributed training optimization
ninja>=1.11.0  # For faster compilation
packaging>=21.0

# Note: Some packages may need to be installed with specific CUDA versions
# For RTX 5090 support, ensure CUDA 12.x compatibility
# Install command examples:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# pip install flash-attn --no-build-isolation